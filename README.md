# discourse-hide-likes
Adds support for hiding likes based on a user custom field.

## Before installing
1. Create a custom user field using the `Confirmation` field type. This could be named "Hide Likes" or something similar.
2. Take note of the user field ID

## Usage
After installing, enter the user field ID in the settings.
